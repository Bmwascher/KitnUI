# KitnUI — local-only files. Everything else is tracked; the shipped player zip
# is shaped by .pkgmeta (which strips dev/, Legacy/, README/CHANGELOG/.github/…),
# not this file. Non-shipped masters + planning docs live under dev/docs/.

# Claude Code / Codex workspace (config, skills, project instructions)
.claude/
.codex/
.agents/
CLAUDE.md
AGENTS.md

# Machine-local tooling: lint / LS config, editor settings, Blizzard UI symlink
.luacheckrc
.wowluarc.json
.vscode/
.idea/
.wow-api-reference

# Third-party reference addons + their index (local-only by convention)
References/
REFERENCES.md

# Local-only dev docs: CurseForge readme, art masters, planning + Superpowers
# artifacts. Tracked dev tooling (dev/Annotations/, dev/README.md) stays in git.
dev/docs/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*.bak
*.tmp
*~
